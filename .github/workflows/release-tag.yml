name: 🚀 Release Tag Version

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      tag:
        description: "Git tag to deploy (optional, e.g. 1.0.0). If empty = deploy current commit on main"
        required: false

jobs:
  build:
    name: "🔨 Build on Runner"
    runs-on: ubuntu-latest

    steps:
      - name: "📥 Checkout (branch or tag)"
        uses: actions/checkout@v4
        with:
          # ถ้ามี inputs.tag ให้ checkout ที่ tag; ไม่งั้นใช้ commit ปัจจุบัน
          ref: ${{ inputs.tag && format('refs/tags/{0}', inputs.tag) || github.sha }}

      - name: "⚙️ Setup Node.js"
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: "📦 Install Dependencies"
        run: npm ci

      - name: "🏗️ Build Project"
        run: npm run build
        # ถ้า Next.js ของคุณตั้ง output: 'standalone' จะดีมากเพราะแพ็กเล็กลง

      - name: "📤 Archive Build Artifacts"
        run: |
          rm -f build.zip
          zip -r build.zip \
            .next \
            public \
            generated/prisma \
            package.json \
            package-lock.json \
            next.config.mjs

      - name: "⬆️ Upload Artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: build.zip

  deploy:
    name: "🚚 Deploy to Server"
    runs-on: ubuntu-latest
    needs: build
    environment: production

    steps:
      - name: "📥 Download Artifacts"
        uses: actions/download-artifact@v4
        with:
          name: build-output

      - name: "🔐 Copy to Server via SCP"
        uses: appleboy/scp-action@v0.1.5
        with:
          host: ${{ secrets.HUAWEI_CLOUD_HOST }}
          username: ${{ secrets.HUAWEI_CLOUD_USERNAME }}
          password: ${{ secrets.HUAWEI_CLOUD_PASSWORD }}
          port: ${{ secrets.HUAWEI_CLOUD_SSH_PORT }}
          source: "build.zip"
          target: "/var/www/sb-web-helper/uploads"

      - name: "🗜️ Unzip, Switch Symlink, Install, Healthcheck (with auto-rollback)"
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.HUAWEI_CLOUD_HOST }}
          username: ${{ secrets.HUAWEI_CLOUD_USERNAME }}
          password: ${{ secrets.HUAWEI_CLOUD_PASSWORD }}
          port: ${{ secrets.HUAWEI_CLOUD_SSH_PORT }}
          script: |
            set -e
            APP_DIR=/var/www/sb-web-helper
            UPLOADS=$APP_DIR/uploads
            RELEASES=$APP_DIR/releases
            mkdir -p "$RELEASES"

            TAG="${{ inputs.tag }}"
            if [ -z "$TAG" ]; then TAG="main"; fi
            TS=$(date +%Y%m%d%H%M%S)
            NEW_RELEASE="$RELEASES/${TS}-${TAG}"

            # เตรียม release ใหม่
            mkdir -p "$NEW_RELEASE"
            cd "$NEW_RELEASE"
            unzip -o "$UPLOADS/build.zip"

            # ลงเฉพาะ production deps (เร็ว+เบา)
            # ถ้าใช้ Next.js standalone ให้ข้ามได้
            npm ci --omit=dev

            # จำ current เดิมไว้ก่อนสลับ
            PREV_RELEASE=$(readlink -f "$APP_DIR/current" || true)

            # สลับ symlink -> release ใหม่
            ln -sfn "$NEW_RELEASE" "$APP_DIR/current"

            # Restart service
            sudo systemctl restart sb-web-helper.service

            # Health Check (แก้ URL ให้ตรงของคุณ)
            # ถ้า reverse proxy เป็น Nginx/PM2 อาจใช้ localhost:PORT ของ service
            if ! curl -fsS http://127.0.0.1:3000/health > /dev/null; then
              echo "❌ Health check failed. Rolling back..."
              if [ -n "$PREV_RELEASE" ]; then
                ln -sfn "$PREV_RELEASE" "$APP_DIR/current"
                sudo systemctl restart sb-web-helper.service
                echo "↩️ Rolled back to $PREV_RELEASE"
              fi
              exit 1
            fi

            echo "✅ Deployed release: $NEW_RELEASE"

            # (ทางเลือก) ล้าง release เก่า เก็บไว้ล่าสุด 10 ชุด
            ls -1dt "$RELEASES"/* | tail -n +11 | xargs -r rm -rf